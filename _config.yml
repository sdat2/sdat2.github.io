remote_theme: pages-themes/slate@v0.2.0
plugins:
- jekyll-remote-theme

markdown: kramdown
kramdown:
  # math_engine: mathjax causes kramdown to emit \(...\) and \[...\] which MathJax 3 handles natively.
  math_engine: mathjax
